export default function LeaderboardSettingsPage() {
  return (
    <div className="card">
      <h1>Leaderboard</h1>
      <p className="muted">
        Leaderboard opt-in is planned after v0.1. It must read public aggregate
        snapshots only, never private raw usage events.
      </p>
    </div>
  );
}
