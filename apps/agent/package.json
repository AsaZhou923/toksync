{
  "name": "@toksync/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "toksync": "./src/index.ts"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@toksync/collector-core": "workspace:*",
    "@toksync/privacy": "workspace:*",
    "@toksync/shared": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
